build_from:
  aarch64: ghcr.io/hassio-addons/base/aarch64:10.1.1
  amd64: ghcr.io/hassio-addons/base/amd64:10.1.1

# Version per GHCR-Tag-Liste verifiziert (ghcr.io/v2/hassio-addons/base/
# {arch}/tags/list) -- der urspruengliche Platzhalter "15.0.9" existierte
# nicht wirklich und war ausserdem veraltet (der erste echte CI-Lauf
# scheiterte daran: das eingefrorene apk-World des alten Images passte
# nicht mehr zu den aktuellen Alpine-Repos). Bei zukuenftigen Problemen
# hier neu pruefen, nicht wieder raten.
